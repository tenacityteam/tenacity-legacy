image: freebsd/12.x
packages:
  - alsa-lib
  - cmake
  - ffmpeg
  - wx28-gtk2-common 
  - gtk2
  - py38-pip
  - gmake
  - conan
  - gcc
sources:
  - https://git.sr.ht/~tenacity/tenacity
tasks:
  - configure: |
      mkdir build
      cd build
      cmake -G "Unix Makefiles" \
            -Daudacity_use_ffmpeg=loaded \
            ../tenacity
  - make: |
      cd build
      make -j4
      # TODO: Add artifacts
